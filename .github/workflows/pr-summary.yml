name: PR Summary

on:
  pull_request:
    types: [opened, synchronize]

permissions:
  contents: read
  pull-requests: write

jobs:
  summarize:
    uses: colus001/workflows/.github/workflows/pr-summary.yml@main
